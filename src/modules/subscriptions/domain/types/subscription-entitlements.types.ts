export type ProjectEntitlements = {
    maxProjectsAmount: number | 'unlimited';
}

export type MembersEntitlements = {
    maxMembersAmount: number | 'unlimited';
}

export type AnalyticsEntitlements = {
    analyticsAvailable: boolean;
}